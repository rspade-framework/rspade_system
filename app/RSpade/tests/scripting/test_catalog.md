# Test catalog: external scripting

| ID | Purpose (what it proves) | Type | Input | Expected (approx) | Status | Last updated |
|----|--------------------------|------|-------|-------------------|--------|--------------|
| SCRIPT-01 | The plain include boots the WHOLE shell from a working directory outside the project: the container is returned, the manifest is loaded, the site `Main::init()` declared is in force, `Rsx_Script::is_active()` is true, and cwd is left at the framework tree | cli | a throwaway script running `require system/script.php`, run from `/tmp` with no arguments | exit 0; JSON with the app class, a resolved `User_Model` path, `site_id` equal to the test process's own and greater than 0, `script_mode` true, `cwd` = `base_path()` | implemented | 2026-09-16 |
| SCRIPT-02 | THE BUG THIS ENTRY POINT EXISTS FOR: a script's first argument is never read as a command name. `help`, `list`, `rsx:manifest:build` and `--version` all skip or special-case the boot under artisan; as a script argument they must change nothing | cli | the same script run with each argument, compared to the no-argument run | exit 0 for each; the JSON is IDENTICAL to the baseline in every field | implemented | 2026-09-16 |
| SCRIPT-03 | A script's own PSR-4 autoloader, registered AFTER the include, resolves a class the manifest has never seen - the chain works because the RSpade autoloader declines rather than throwing | cli | a script that registers a loader for a class file written beside it | the script's class resolves; a framework class still resolves; an unknown class answers `class_exists() === false` rather than throwing | implemented | 2026-09-16 |
| SCRIPT-04 | Maintenance mode refuses the WHOLE process (not a classified command), naming the reason, what it refused, the `$RSX_SCRIPT_OPTIONS['force']` override and the exit command, at exit 75 - and a forced script boots normally | cli | real flag raised with `--no-services`; the plain probe, then a probe setting `$RSX_SCRIPT_OPTIONS = ['force' => true]` | refusal marker + reason + `external scripts are refused` + both hints, exit 75; forced run exits 0 with a full boot | implemented | 2026-09-16 |
| SCRIPT-05 | Artisan's own behaviour did not move when its pre-boot sequence became a shared function: the interceptions, the `--_` strip, the test-run declaration and the allow-most-deny-some classification | cli | the existing `maintenance/cli/Maintenance_Gate_Cli_Test.php` (M-12..M-19) and the framework-update concern's subprocess gate tests | unchanged | covered elsewhere | 2026-09-16 |
| SCRIPT-06 | The container gate echoes the SCRIPT's invocation rather than an artisan command line when it refuses one | cli | a script run on a box with no `/.rspade_container` in development mode | the refusal prints `php <script path and args>` | deferred (needs a box outside the container; the branch is one `defined('RSX_SCRIPT_MODE')` read in `rsx_container_gate_refuse()`) | 2026-09-16 |
| SCRIPT-07 | The entry point refuses a non-CLI SAPI | cli | including `system/script.php` under a web SAPI | stderr refusal naming `system/public/index.php`, exit 1 | deferred (no web SAPI reachable from the runner; the branch is a `PHP_SAPI !== 'cli'` guard executed before anything else) | 2026-09-16 |
