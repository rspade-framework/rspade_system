# test_runner - test catalog

| ID | Purpose (what it proves) | Type | Input | Expected (approx) | Status | Last updated |
|----|--------------------------|------|-------|-------------------|--------|--------------|
| TR-01 | `merge_and_report()` prints the sequential format line for line: `Running: X`, `  [OK] t`, `  - t (skipped)` + message, `  [FAIL] t` + message + `    at f:l`, `  Error running test class: ...`, the missing-record `[FAIL]` + "class produced no result (worker terminated before it finished)", the `Test Summary` block and `Tests failed!` | php | synthetic `results.jsonl` with a passing+skipped class, a failing class, an errored class, and a selected class with no record | every line matches exactly; exit code 1 | implemented | 2026-09-05 |
| TR-02 | A class-level throw and a class that produced nothing are both FAILURES, and neither adds to `Total` (which counts methods that answered) | php | the same synthetic file | `Total: 3`, `Failed: 3` | implemented | 2026-09-05 |
| TR-03 | The sequential printer and the docker merge emit identical bytes for identical results - the one-printer guarantee | php | the same results pushed through `print_class_results()` directly | byte-identical prefix | implemented | 2026-09-05 |
| TR-04 | `print_summary()` exit codes and wording: clean run 0 + "All tests passed!", empty run 0 + "No tests were run." | php | totals arrays | as stated | implemented | 2026-09-05 |
| TR-05 | `--filter` narrows METHODS in the merge exactly as in the sequential loop, and a class whose NAME matched reports every method | php | filtered `print_class_results()` calls | one method printed and counted; class match reports both | implemented | 2026-09-05 |
| TR-06 | `order_classes_longest_first()`: a measured timing outranks everything | php | timings file naming all three fixtures | descending by measured duration | implemented | 2026-09-05 |
| TR-07 | Without timings, `$requires_db_reset` classes lead (they carry the re-provision cost) | php | no timings file | the reset fixture first | implemented | 2026-09-05 |
| TR-08 | Equal scores are stable in incoming order, so seeding is reproducible | php | no timings file, two identically-scored fixtures | incoming order preserved | implemented | 2026-09-05 |
| TR-09 | Ordering never adds or loses a class | php | with and without timings | the same set both ways | implemented | 2026-09-05 |
| TR-10 | The docker gate OPENS only for the whole framework suite with no selector, not `--sequential`, in a dev container, with docker answering | php | gate called with those arguments | true (skipped outside a dev container) | implemented | 2026-09-05 |
| TR-11 | Every narrowing selector closes the gate: a named class, `--filter`, `--group` | php | gate called per selector | false | implemented | 2026-09-05 |
| TR-12 | `--sequential` closes the gate | php | gate with the option set | false | implemented | 2026-09-05 |
| TR-13 | The application suite closes the gate | php | `framework_only = false` | false | implemented | 2026-09-05 |
| TR-14 | An unusable docker daemon closes the gate quietly - a box without docker still runs its tests | php | stubbed `docker_is_usable()` returning false | false, no throw | implemented | 2026-09-05 |
| TR-15 | The worker count stays inside `min(8, cores, floor(RAM_MB/1000))`, floors at 1, and never exceeds the class count | php | real `/proc`, class counts 0/1/3/1000 | 1..8, capped by class count | implemented | 2026-09-05 |
| TR-16 | `--workers=N` overrides the formula but still honours both floors | php | `--workers=32` and `--workers=0` | 32 capped by class count; nonsense falls back to the formula | implemented | 2026-09-05 |
| TR-17 | A second runner is refused the singleton flock while a run holds it | php | `php -r` subprocess, `LOCK_EX \| LOCK_NB` on the live lock file | BLOCKED | implemented | 2026-09-05 |
| TR-18 | The lock file names its holder pid | php | read the live lock file | a pid | implemented | 2026-09-05 |
| TR-19 | The lock is granted again the moment it is released | php | scratch lock file, held then released | BLOCKED then GOT | implemented | 2026-09-05 |
| TR-20 | The queue answers `ping`, hands out classes in seeded order with `short` + `requires_db_reset`, then `{class:null}` | cli | scratch `Queue_Server` on a temp socket, 3 seeded classes | as stated | implemented | 2026-09-05 |
| TR-21 | The holder map attributes a class to the worker that took it, and clears on `queue.result` (dead-worker attribution) | cli | `held_by()` before and after | 2/1 then 0/0 | implemented | 2026-09-05 |
| TR-22 | `queue.result` acknowledges `{}`, `[]` and an errored record alike | cli | three result frames | `{ok:true}` each | implemented | 2026-09-05 |
| TR-23 | A `results.jsonl` record carries EXACTLY class/short/results/duration (+error) and never `worker_id`; `{}` stays an object and `[]` stays an array | cli | the written file | exact key sets | implemented | 2026-09-05 |
| TR-24 | An unknown method, a malformed frame and an oversized frame are all errors, and the server keeps serving afterwards | cli | abusive frames then a `ping` | error answers, then `pong` | implemented | 2026-09-05 |
| TR-25 | The socket file is removed on close | cli | `Queue_Server.close()` | the path is gone | implemented | 2026-09-05 |
| TR-26 | The worker wrapper waits for the whole supervisor roster and an rsx-lockd ping before any test runs | cli | a container whose services are still STARTING | no test runs until every program is RUNNING | deferred - it needs a real container and a real supervisor; asserted instead by every parallel run, whose worker logs carry "all supervised services are RUNNING (Ns)" | 2026-09-05 |
| TR-27 | The wrapper fails loud (non-zero, with `supervisorctl status`) rather than waiting forever on a FATAL program or a dead supervisord | cli | a container with a program forced to FATAL | exit 1 naming the program | planned - needs a fixture image that can park a program in FATAL | 2026-09-05 |
| TR-28 | The zombie sweep kills and removes labelled leftovers before the build | cli | a labelled `sleep` container left running | gone before the build starts | deferred - docker lifecycle, exercised by the epic's acceptance run rather than a unit test | 2026-09-05 |
| TR-29 | Teardown after Ctrl+C: no labelled containers, no stray `rspade-test:*` tags beyond `:latest`, the generated `.dockerignore` removed, the flock released | cli | SIGINT mid-run | a clean box | deferred - same reason; verified manually at epic acceptance | 2026-09-05 |
| TR-30 | A container that dies mid-class produces the synthetic "worker terminated" record rather than a silent drop | php | orchestrator-level fault injection | the class is reported FAILED | deferred - the FORMAT half is covered by TR-01; the injection half needs a real container to kill | 2026-09-05 |
| TR-31 | The image's baked datadir template is a cleanly shut-down database (no crash recovery in any container) | cli | inspect a fresh container's mysqld error log | no InnoDB recovery lines | planned | 2026-09-05 |
| TR-32 | A container's compiled bundle output is present, so the asset tests that read it assert rather than skip | asset | the four `Bundle_Emission_Order_Test` / `Externals_Bundle_Map_Test` methods | no "No compiled X Bundle app JS present" skips in a docker run | implemented - by the image, which now compiles bundles at build time; asserted by those tests themselves | 2026-09-05 |
| TR-33 | A full-suite verdict written under a build key reads back whole (records, exit code, run dir, timestamp) | php | `write_cached_results` then `read_cached_results` with the same key | identical record | implemented | 2026-09-07 |
| TR-34 | A verdict recorded under one build key is never a hit for another; a missing, unreadable or incomplete file is "not yet run" | php | `read_cached_results` with a mismatched key / no file / garbage / partial JSON | null in every case | implemented | 2026-09-07 |
| TR-35 | The cache path is keyed by the build key and results.jsonl parsing skips malformed lines | php | `results_cache_path('deadbeef')`, a jsonl with two bad lines | `.../rsx-tmp/test-results/framework_deadbeef.json`; two records | implemented | 2026-09-07 |
| TR-36 | A second full docker run under an unchanged build key replays the cached verdict without building an image or starting a container | cli | two consecutive `rsx:test --framework` runs | second run prints "Returning cached test results for build <key> from <file>" and the same summary | implemented - exercised by hand at epic end (needs docker) | 2026-09-07 |
| TR-37 | The environment fingerprint that joins the manifest build key in the result-cache key follows file name, size and mtime and ignores a missing directory | php | `fingerprint_directories()` over a scratch tree | changes on size/mtime/new file; sha1('') for a missing dir | implemented | 2026-09-07 |
