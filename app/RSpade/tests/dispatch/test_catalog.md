# Dispatch test catalog

| ID | Purpose (what it proves) | Type | Input | Expected (approx) | Status | Last updated |
|----|--------------------------|------|-------|-------------------|--------|--------------|
| DISP-01 | B4.4: a throwing non-controller `pre_dispatch` is NOT swallowed | php | non-controller handler whose static `pre_dispatch` throws | exception propagates out of `__call_action` (where the non-controller `pre_dispatch` runs) | implemented | 2026-07-30 |
| DISP-02 | B4.4: a null-returning `pre_dispatch` still lets dispatch proceed | php | non-controller handler, `pre_dispatch` returns null | `__call_action` proceeds to the action | implemented | 2026-07-30 |
| DISP-03 | B4.6: AUTH_REQUIRED full-page GET redirects to login with intended URL | php | `Error_Response(ERROR_AUTH_REQUIRED)`, GET the concern's routed page fixture, logged out | `RedirectResponse` to `/login` containing the URL-encoded fixture path as `redirect=` | implemented | 2026-09-08 |
| DISP-04 | B4.6: UNAUTHORIZED while logged out redirects to login | php | `Error_Response(ERROR_UNAUTHORIZED)`, logged out | `RedirectResponse` to `/login` | implemented | 2026-07-30 |
| DISP-05 | B4.6: UNAUTHORIZED while logged in is a 403, not a redirect or 500 | php | `Error_Response(ERROR_UNAUTHORIZED)`, acting as a seeded user | `HttpException` with status 403 | implemented | 2026-07-30 |
| DISP-06 | Channel split: the ajax handler throws unauthorized (JSON), never redirects | php | `Ajax::_handle_special_response(Error_Response(ERROR_UNAUTHORIZED))` | throws `AjaxUnauthorizedException` | implemented | 2026-07-30 |
| DISP-07 | Wire-level split: a logged-out full-page GET to a gated route 302s to login, and an underscore-led framework path is NOT threaded back | http | GET `/_sys` (logged out) | 302 to `/login`, no `redirect=` (Login_Redirect drops a non-page target; its accept half is `Login_Redirect_Test`) | implemented | 2026-09-08 |
| DISP-08 | Wire-level split: same rejection via ajax endpoint keeps JSON contract | http | POST `/_ajax/Rsx_Timezone_Controller/get_settings` (logged out) | no redirect, body has `error_code:unauthorized` | implemented | 2026-09-08 |
| DISP-09 | Dev auth: a `#fragment` in the rsx:debug url is not signed - the harness still renders AUTHENTICATED | http | `rsx:debug /_sys` and `rsx:debug '/_sys#foo=bar'` with `--user=1` | both bodies carry `"is_auth": true`; the fragmented run navigates to `/_sys#foo=bar` | implemented | 2026-09-08 |
| DISP-10 | abort(404) in an RSX action is a 404, not the uncaught-fatal 500 it used to be | php | dispatch `/_test/dispatch/abort-404` | 404 | implemented | 2026-08-22 |
| DISP-11 | abort(403) for a SIGNED-IN caller is a real 403 | php | acting as user 1, dispatch `/_test/dispatch/abort-403` | 403 | implemented | 2026-08-22 |
| DISP-12 | abort(403) for an ANONYMOUS caller keeps the existing login-redirect split | php | logged out, dispatch `/_test/dispatch/abort-403` | 302 to `/login` | implemented | 2026-08-22 |
| DISP-13 | A status with no Error_Screens page keeps its own status and message | php | dispatch `/_test/dispatch/abort-418` | 418, body carries the abort message | implemented | 2026-08-22 |
| DISP-14 | Asset channel (Accept does not prefer html) gets a plain body, never a themed page | php | `Accept: image/webp`, dispatch `/_test/dispatch/abort-404` | 404, message body, no `<html` | implemented | 2026-08-22 |
| DISP-15 | The catch is narrow: a non-HTTP exception still propagates out of dispatch | php | dispatch `/_test/dispatch/throw` | `RuntimeException` escapes | implemented | 2026-08-22 |
| DISP-16 | Wire-level: an unknown preview rendition key is 404, on both the browsed and asset channels, and abort(418) arrives as 418 | http | GET `/_preview/pdf/doesnotexist`, `/_test/dispatch/abort-418` | 404 / plain 404 / 418 | implemented | 2026-08-22 |
| DISP-17 | Dev-auth verifies against the newest grant, and still against the PREVIOUS one after a rotation | php | mint, then `Ide_Bridge_Token::rotate()` | both verify (rotation is never a forgery) | implemented | 2026-09-06 |
| DISP-18 | A credential signed by a RETIRED (third-oldest) grant is refused | php | mint, rotate twice | rejection reason returned | implemented | 2026-09-06 |
| DISP-19 | An EXPIRED credential is refused, and the exp cannot be extended on the wire | php | `exp = time()-1`; then a rewritten exp header | 'expired' / 'signature mismatch' | implemented | 2026-09-06 |
| DISP-20 | A missing/malformed `X-Dev-Auth-Exp` or a missing token is refused | php | null, '', 'soon', '-5', '1.5' | all rejected | implemented | 2026-09-06 |
| DISP-21 | With no grant store nothing mints and nothing verifies | php | store deleted | `mint()` null, `active_secrets()` [] , verify rejects | implemented | 2026-09-06 |
| DISP-22 | Dev-auth is DEVELOPMENT ONLY - a sealed debug build (Laravel says 'local') refuses it | php | `Rsx::_testing_set_mode(debug/production)` | rejection names 'development-only' | implemented | 2026-09-06 |
| DISP-23 | The credential is scoped to one url + user + realm | php | verify against another url / user / portal flag | all three rejected | implemented | 2026-09-06 |
| DISP-24 | The signed payload is the documented wire format (key order + PHP `\/` escaping) | php | `Dev_Auth_Token::payload('/_sys', ...)` - the URL is an opaque HMAC input and never has to route | exact JSON string | implemented | 2026-09-08 |
| DISP-25 | `is_loopback_ip()`: a proxied LOCAL request is loopback, a proxied REMOTE one is not, and forwarding with no declared client fails closed | php | synthetic REMOTE_ADDR + `X-Forwarded-For` / `X-Real-IP` / `X-Forwarded-Host` | true / false / false | implemented | 2026-09-06 |
| DISP-26 | The plain-text trace handler answers the local harness and refuses a non-loopback or forwarded-remote caller, and any non-development mode | php | `Playwright_Exception_Handler::handle()` | response with trace / null / null | implemented | 2026-09-06 |
| DISP-27 | The `X-Playwright-Console-Debug` override answers the local harness only | php | private predicate on `Debugger` | true / false (remote, proxied-remote, debug mode) | implemented | 2026-09-06 |
| DISP-28 | A correctly signed credential whose expiry lies more than one lifetime out is refused (the cap is verifier-enforced) | php | sign with a live grant, exp = now + 86400 | rejection names the lifetime cap | implemented | 2026-09-06 |
| DISP-29 | A build-artifact miss (`/_vendor/`, `/_compiled/`) is a plain-text 404 RESPONSE on the ASSET channel - never a thrown 404, never a route | php | `Rsx_Front_Controller::handle()` on both misses | 404, `text/plain`, channel `asset` | implemented (`Front_Controller_Test`) | 2026-09-25 |
| DISP-30 | A 404 raised after the action returned (outside the action's seam) is rendered once: a 404, the action run once | php | `/_test/front/abort-after-action` | 404; `$invocations === 1`; not in flight afterwards | implemented (`Front_Controller_Test`) | 2026-09-25 |
| DISP-31 | Entering the front controller again during a dispatch is refused | php | `/_test/front/reenter` | inner call throws naming "re-entered"; outer request 200 | implemented (`Front_Controller_Test`) | 2026-09-25 |
| DISP-32 | Laravel routes are unreachable (the router is not in the request path) | php + http | `/_ignition/health-check`, POST `/_ignition/execute-solution`, `/sanctum/csrf-cookie`, `/test-bundle-facade` | all 404 | implemented (`Front_Controller_Test`, `http/front_controller_404s.sh`) | 2026-09-25 |
| DISP-33 | The external API answers 404 not_found on the portal's dedicated domain, and 401 on the staff host | php | `rsx.portal.domain` set; `/api/v1/me` on each host | 404 JSON `not_found` / 401 | implemented (`Front_Controller_Test`) | 2026-09-25 |
| DISP-34 | Channel classification: ASSET / API / AJAX (POST only) / PAGE, the realm by prefix or domain, the API's realm always staff, artifacts under the portal prefix | php | synthetic requests, both portal shapes | per-row channel/realm/portal_host | implemented (`Request_Channel_Test`) | 2026-09-25 |
| DISP-35 | Default route: GET redirects to the real URL (params from the query), POST runs only a route that accepts POST | php | `/_/Front_Controller_Fixture_Controller/get_only`, `get_post?id=7` | 302 / 200 JSON; GET never runs the method | implemented (`Default_Route_Test`) | 2026-09-25 |
| DISP-36 | Default route refuses: POST to a GET-only route, a non-scalar query value, an Ajax endpoint, a non-controller or unknown class, an SPA bootstrap, an /error/ page handler | php | each shape | 404, method never run | implemented (`Default_Route_Test`) | 2026-09-25 |
| DISP-37 | Over HTTP, a build-artifact miss is a plain-text 404 and an unknown page a 404, each in one dispatch | http | curl the misses | 404 `text/plain` / 404 html | implemented (`http/front_controller_404s.sh`) | 2026-09-25 |
| DISP-38 | Direct and batched Ajax calls to the same endpoint build the identical envelope for success, validation, a thrown unauthorized, not-found, abort(404) and a crash | php | `Ajax::handle_browser_request` vs `Ajax::handle_batch_request` on each `Ajax_Parity_Fixture_Controller` endpoint | envelopes equal (fatal `error_code` `fatal`) | implemented (`Ajax_Transport_Parity_Test`) | 2026-09-25 |
| DISP-39 | A batched endpoint runs on the real request: the caller's IP and headers, with only its own params as input | php | batch call to the `caller` fixture | real IP + header, the call's own params | implemented (`Ajax_Transport_Parity_Test`) | 2026-09-25 |
| DISP-40 | The AJAX channel dispatches both transports through the front controller; an unknown `/_ajax/` path is the not_found envelope | php | POST `/_ajax/<Fixture>/succeed`, `/_ajax/_batch`, `/_ajax/nope` | success envelope / batch map / `error_code` `not_found` | implemented (`Ajax_Transport_Parity_Test`) | 2026-09-25 |
| DISP-41 | A batch over `rsx.ajax.batch_max_calls` is refused whole | php | cap lowered, one call over it | 400 naming the key; `$invocations === 0` | implemented (`Ajax_Transport_Parity_Test`) | 2026-09-25 |
| DISP-42 | A malformed batch is refused whole: call_id array / string / negative / repeated, a missing action, non-object params | php | one malformed call beside a valid one | 400; nothing runs | implemented (`Ajax_Transport_Parity_Test`) | 2026-09-25 |
| DISP-43 | Main::pre_dispatch runs once per Ajax request (not once per batched call) with the synthetic keys, and a non-null return halts it | php | `Ajax_Main_Hook_Probe` as Main; one direct call and one two-call batch; then a halting probe | two recordings (`_route`, `_handler`, `_method`); halted: 409, no endpoint ran | implemented (`Ajax_Transport_Parity_Test`) | 2026-09-25 |
| DISP-44 | A thrown AjaxUnauthorizedException on a page is the unauthorized screen, never a 500 | php | dispatch `/_test/front/thrown-denial` logged out, then signed in | 302 to login / 403 | implemented (`Coded_Failure_Page_Test`) | 2026-09-25 |
| DISP-45 | The page policy maps the coded family, and a HEAD failure keeps its status with no body | php | `Web_Exception_Handler` with `AjaxUnauthorizedException`, `AjaxNotFoundException`; a HEAD request | 403 / 404; HEAD status kept, empty body | implemented (`Coded_Failure_Page_Test`) | 2026-09-25 |
| DISP-46 | ROUTE-VERB-01: a route declares GET and/or POST only, in both realms | php | `#[Route]` / `#[Portal_Route]` with GET, POST, both, omitted; PUT, DELETE, PATCH, HEAD, OPTIONS, empty list | first four accepted; the rest FATAL naming ROUTE-VERB-01 | implemented (`Route_Verb_Rule_Test`) | 2026-09-25 |
| DISP-47 | Default route: GET naming a staff JS SPA action redirects to its @route URL; POST, another action name and an unknown action are refused | php | GET `/_/_Sys_Dashboard_Action/index`; POST it; `/_/_Sys_Dashboard_Action/other`; unknown action | 302 to `/_sys`; 404 each, nothing run | implemented (`Default_Route_Test`) | 2026-09-25 |
| DISP-48 | An excluded public file (an `rsx.public.ignore_patterns` match) answers exactly as a missing one | http | a `*.sh` probe file in the root public/ vs a missing name | identical 404 status and type | implemented (`http/front_controller_404s.sh`) | 2026-09-25 |
| DISP-49 | API rows in the staff route table are never resolved by the page dispatcher | php | `Dispatcher::resolve_url_to_route('/api/v1/me')` | null (the row exists; only Api_Dispatcher serves it) | implemented (`Front_Controller_Test`) | 2026-09-25 |
| DISP-50 | Rsx::Route replaces each :token whole and by name, fills or drops an optional `:x?` with its slash, demands only required tokens, prefers the pattern filling the most tokens | php | `/x/:id/:id_two`, `/list/:page?`, `/a/:id/b/:tab?`, selection over three patterns | `/x/1/2`, `/list/3` / `/list`, `/a/5/b`, missing `[id]` only | implemented (`Route_Url_Tokens_Test`) | 2026-09-25 |
| DISP-51 | The JS twin Rsx._generate_url_from_pattern / _select_best_route_pattern answers the same cases | playwright | the DISP-50 cases evaluated in `/_sys` | identical URLs | implemented (`playwright/route_url_tokens.js`) | 2026-09-25 |
| DISP-52 | Rsx::Route's hash argument: encoded as encodeURIComponent (the JS serializer), query then hash keys then the `at` anchor, null/'' dropped, no '#' when empty, placeholder ignores it; a non-string/int value, a list or integer key, `at` inside the hash and invalid UTF-8 throw | php | `Dispatch_Page_Fixture_Controller::page` with the PARITY_URL vector and each invalid shape | the PARITY_URL literal; `Rsx_Caller_Exception` per invalid shape | implemented (`Route_Hash_Test`) | 2026-09-25 |
| DISP-53 | The JS twin: byte-identical to the PHP PARITY_URL, round-trips through Rsx.url_hash_get(), Rsx_Portal.Route carries the same fragment, and the same invalid shapes throw | playwright | probe routes written into `Rsx._routes` / `Rsx_Portal._routes` on `/_sys` | identical URL; every value read back unchanged | implemented (`playwright/route_hash.js`) | 2026-09-25 |
