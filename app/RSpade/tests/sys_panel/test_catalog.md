# sys_panel - test catalog

| ID | Purpose (what it proves) | Type | Input | Expected (approx) | Status | Last updated |
|----|--------------------------|------|-------|-------------------|--------|--------------|
| RP-SPA-01 | Every panel screen is a SPA route on `_Sys_Spa_Controller`, gated `is_sysadmin`, served by its own action | php | Persisted route index | every catalogued pattern, `type=spa`, `surface_gates(surface)` and `surface_gates(target)` contain `is_sysadmin` | implemented | 2026-09-07 |
| RP-SPA-02 | The panel has EXACTLY its declared SPA routes (the seven screens plus `/_sys/tasks/:id`, `/_sys/email/:id`, `/_sys/email/sms/:id`, `/_sys/logs/:file`, `/_sys/sites/:id` and `/_sys/users/:id`) - another cannot arrive unnoticed | php | Persisted route index | the pattern set equals the catalog's | implemented | 2026-09-26 |
| RP-SPA-03 | Every panel action declares `_Sys_Layout` | php | Action file metadata | `@layout` == `['_Sys_Layout']` | implemented | 2026-09-07 |
| RP-ROUTE-01 | `/_sys/logout` is a GET route on `_Sys_Controller`, gated `is_sysadmin` | php | Persisted route index | `type=standard`, methods `['GET']` | implemented | 2026-09-07 |
| RP-BUNDLE-01 | Neither panel bundle names an `rsx/` path (CONV-BUNDLE-04, stated in the suite) | php | `define()` of both bundles | no include/watch entry under `rsx/` | implemented | 2026-09-07 |
| RP-BUNDLE-02 | `_Sys_Bundle` includes the panel's theme bundle, theme dir and its own module dir | php | `_Sys_Bundle::define()` | all three present | implemented | 2026-09-07 |
| RP-BUNDLE-03 | The theme bundle compiles: Bootstrap arrives through the `/vendor/` `@import`, the palette overrides win | php | `BundleCompiler::compile()` | CSS carries `.btn`, `--rsx-accent` and `#58a6ff` | implemented | 2026-09-07 |
| RP-APIDOCS-01 | The API console's bundle names no `rsx/` path (CONV-BUNDLE-04, stated in the suite) | php | `_Apidocs_Bundle::define()` | no include/watch entry under `rsx/` | implemented | 2026-09-07 |
| RP-APIDOCS-02 | The console wears the framework application's theme instead of a private palette | php | `_Apidocs_Bundle::define()` | `_Sys_Theme_Bundle` and `app/RSpade/Sys/theme` both included | implemented | 2026-09-07 |
| RP-APIDOCS-03 | The manifest holds `_Apidocs_App` (blade id) and `_Apidocs_Console` (jqhtml), at their console-module paths | php | Manifest view + jqhtml index | both resolve under `app/RSpade/Sys/app/apidocs/` | implemented | 2026-09-07 |
| RP-APIDOCS-04 | `Core/Api/` is engine-only: no `.jqhtml`, `.blade.php` or `.scss` under it | php | Recursive scan of `app/RSpade/Core/Api` | empty offender list | implemented | 2026-09-07 |
| RP-CONFIG-01 | `rsx.sys_panel.enabled = false` makes `/_sys` NOT FOUND, for a user who would otherwise pass | php | `Dispatcher::dispatch('/_sys')` as user 1 | 404 | implemented | 2026-09-07 |
| RP-CONFIG-02 | Enabled, a signed-in identity gets the panel on its own bundle | php | `Dispatcher::dispatch('/_sys')` as user 1 | 200, body names `_Sys_Bundle` | implemented | 2026-09-07 |
| RP-AUTH-01 | An anonymous caller is sent to login, not shown a 403 | php | `Dispatcher::dispatch('/_sys')`, no session | 302 to `/login` | implemented | 2026-09-07 |
| RP-LOGOUT-01 | `/_sys/logout` clears the session identity and redirects to `/` | php | `Dispatcher::dispatch('/_sys/logout')` as user 1 | 302 to `/`, `Session::is_logged_in()` false | implemented | 2026-09-07 |
| RP-TOOLS-01 | `rsx:manifest:show --classes` hides `_Sys_*` from an application developer, shows it to a framework developer | php | `Artisan::call` with the flag both ways | no `_Sys_` when false, `_Sys_` when true, ordinary classes unaffected | implemented | 2026-09-07 |
| RP-TOOLS-02 | `rsx:routes` hides every `/_sys` route from an application developer | php | `Artisan::call` with the flag both ways | no `/_sys` when false, eight when true, `/login` unaffected | implemented | 2026-09-07 |
| RP-TOOLS-03 | `rsx:manifest:show --files` hides the tree's classless files (.jqhtml, .scss) by PATH | php | `Artisan::call` with the flag both ways | no `app/RSpade/Sys/` when false, present when true | implemented | 2026-09-07 |
| RP-TOOLS-04 | The predicate itself: a reserved NAME and a `Sys/` PATH follow the flag, ordinary ones never do | php | `Rsx_Identifier::is_visible_to_developer` / `is_path_visible_to_developer` | flag false hides both, flag true shows both | implemented | 2026-09-07 |
| RP-UI-01 | The midnight palette paints and Bootstrap components wear it | playwright | `/_sys` as a signed-in user | sidebar surface, accent-soft active item, themed `.btn-primary` and `.card` | not implemented | 2026-09-07 |
| RP-UI-02 | The sidebar's active item follows SPA navigation without a reload | playwright | `/_sys` -> `/_sys/users` | `.active` moves, no full page load | not implemented | 2026-09-07 |
| RP-UI-03 | A denied screen's nav entry does not render (nav honesty) | playwright | a screen whose gate denies | the link is absent | not implemented | 2026-09-07 |
| RP-PUB-01 | The panel's INDEX ACTION (not its bootstrap controller) is the configured always-published target, and it resolves in the manifest | php | `config('rsx.always_published_routes')` + `routes_by_target` | `_Sys_Dashboard_Action` present with patterns; `_Sys_Spa_Controller::index` has none | implemented | 2026-09-07 |
| RP-PUB-02 | A compiled application bundle carries the published route table with the pattern resolved FROM THE MANIFEST, and no panel code | php | in-process `BundleCompiler::compile()` of the first application module bundle the manifest declares | generated JS contains `Rsx._define_published_spa_routes` + the target + each manifest pattern and `Manifest._define_published_subclass_index`; no `_Sys_Layout` | implemented | 2026-09-08 |
| RP-PUB-03 | A configured target with no routes FAILS THE COMPILE rather than silently vanishing | php | `_collect_always_published_routes` with a bogus target | RuntimeException | implemented | 2026-09-07 |
| RP-PUB-04 | `auth_routes_published` is always present and carries explicit denials: 1 signed in, 0 anonymous | php | `export_published_route_grants('staff')` before/after `__acting_as_user(1)` | key present both times; 0 then 1 | implemented | 2026-09-07 |
| RP-PUB-05 | The map is realm-scoped - a staff target is never answered for in the portal realm | php | `export_published_route_grants('portal')` | key absent | implemented | 2026-09-07 |
| RP-ERR-01 | The panel bundle registers its own error screens, and `Error_Screens.not_found()` mounts the registered one in the layout | playwright | `/_sys` as user 1, `Error_Screens.get_components()` + `not_found()` | `_Sys_Unauthorized`/`_Sys_Not_Found`/`_Sys_Error`; `._Sys_Not_Found` inside `$sid=content` | implemented | 2026-09-26 |
| RP-ERR-02 | A bundle with nothing registered fails loud | playwright | `_components = null`, `not_found()` | throws naming `set_components` | implemented | 2026-09-26 |
| RP-ERR-03 | A registered name the bundle does not carry fails loud | playwright | a bogus `not_found` name | throws naming the component | implemented | 2026-09-26 |
| RP-ERR-04 | `set_components()` refuses an incomplete set | playwright | `{unauthorized, not_found}` | throws naming `fatal` | implemented | 2026-09-26 |
| RP-MODAL-01 | `_Sys_Modal.confirm()`: Enter accepts | playwright | Enter with focus on the dialog | resolves true | implemented | 2026-09-26 |
| RP-MODAL-02 | Cancel and Escape both resolve false | playwright | Cancel button; Escape key | false, false | implemented | 2026-09-26 |
| RP-MODAL-03 | Only a literal false from a callback keeps a dialog open; null closes | playwright | `show()` with a false and a null callback | open after false; resolves null | implemented | 2026-09-26 |
| RP-MODAL-04 | Enter in a textarea is a newline, not an accept | playwright | Enter in a textarea body, then on the dialog | newline, open; then resolves the default value | implemented | 2026-09-26 |
| RP-MODAL-05 | A one-argument `alert()` is body-only; OK resolves true | playwright | `alert('Only a body')` | no header, body text, true | implemented | 2026-09-26 |
| RP-MODAL-06 | `form()` around a component with no `<Rsx_Form>` fails loud and stays open; dismissal resolves false | playwright | `form({component: '_Sys_Empty_State'})` | page error naming `<Rsx_Form>`, dialog open, then false | implemented | 2026-09-26 |
| RP-MODAL-07 | `form()` submit round trip: a server validation failure keeps the dialog open with field errors; success resolves the result | playwright | the Tasks Kill dialog (`_Sys_Task_Kill_Form` -> `_Sys_Tasks_Controller.kill`) against a planted RUNNING row with no pid | open + `.is-invalid`, the `<textarea>` itself painted in `--bs-form-invalid-border-color` (the mark lands on the input component; its SCSS carries it to the control); then `{id, outcome: killed_no_process}` and the row KILLED with the explanation | implemented | 2026-09-26 |
| RP-GATE-01 | Every auth surface indexed from `app/RSpade/Sys/app/sys/` carries `is_sysadmin` and is staff-realm, save the ONE named exception, `_Sys_Impersonation_Controller::stop` (GET /_sys/stop-impersonating), which carries exactly `['is_logged_in']` and is staff-realm; the exception list has exactly one entry | php | `Auth_Gates::get_surfaces()` filtered by file | `is_sysadmin` in every other `auth` | implemented | 2026-09-26 |
| RP-GATE-02 | The index is complete: every PHP method with a surface attribute and every JS class with `@route` under the module has an indexed surface | php | `Manifest::get_all()` vs the surfaces | every declared target indexed | implemented | 2026-09-26 |
| RP-GATE-03 | Every panel controller extends `_Sys_Endpoint_Controller_Abstract` (the inherited enabled switch) | php | concrete controllers under the module | `php_is_subclass_of` true | implemented | 2026-09-26 |
| RP-GATE-04 | A signed-in identity with an enabled membership that is not a developer is refused `/_sys` | php | `Dispatcher::dispatch('/_sys')` | 403 | implemented | 2026-09-26 |
| RP-GATE-05 | The same identity is refused one Ajax endpoint of every panel controller that declares any, through the browser transport with a valid CSRF token | php | `Rsx_Front_Controller::handle` POST `/_ajax/C/m` | `error_code` unauthorized, never a CSRF refusal | implemented | 2026-09-26 |
| RP-GATE-06 | The inherited switch on the Ajax channel: a panel-shaped fixture controller answers a developer, answers not_found when the panel is disabled, and refuses a non-developer | php | `Sys_Panel_Endpoint_Fixture_Controller::ping` | success; not_found; unauthorized | implemented | 2026-09-26 |
| RP-DEBUG-01 | `Console_Debug_Override::normalize()` is the one validator: channels normalized, deduped, sorted; 'all' carries none; switches become booleans; a bad mode, a narrowing mode with no channel, a nameless channel or a non-list is refused | php | `normalize()` / `validate()` | canonical array; errors keyed `filter_mode` / `channels`; `normalize()` throws | implemented | 2026-09-26 |
| RP-DEBUG-02 | A developer's stored override is what PHP and JS run under: `Debugger::effective_console_config()` and `rsxapp.console_debug` carry it (specific joins its channels), the outputs stay the configuration's | php | `store()` as user 1, `apply_session_override()`, `console_debug_payload()` | whitelist/specific values and switches from the override; outputs unchanged | implemented | 2026-09-26 |
| RP-DEBUG-03 | The front controller applies it: a page dispatched for the developer's session renders the override into its rsxapp | php | `Rsx_Front_Controller::handle` GET `/_sys` as user 1 | 200; `window.rsxapp.console_debug` carries the override; `Debugger::session_override()` set | implemented | 2026-09-26 |
| RP-DEBUG-04 | The same stored value under a NON-developer session does nothing | php | non-developer identity, `store()`, `apply_session_override()` | `active()` null; PHP config and rsxapp payload identical to the unstored baseline | implemented | 2026-09-26 |
| RP-DEBUG-05 | An anonymous request resolves no override and creates no session doing so | php | no session, `apply_session_override()` | override null; `has_session()` false; `_sessions` count unchanged | implemented | 2026-09-26 |
| RP-DEBUG-06 | The endpoints: `save` refuses an invalid override with a field error and stores a valid one; `state` reports it with source `browser` for every replaced key and never for `outputs.*`; `channels` offers a stored channel the scan does not find; `reset` removes it | php | `_Sys_Debug_Flags_Controller` as user 1 | Error_Response with `channels`; the normalized override; sources; `found: false`; null after reset | implemented | 2026-09-26 |
| RP-DEBUG-07 | The channel inventory is one scan: `Console_Debug_Channels::scan()` returns exactly what `rsx:console_debug:list_channels` prints | php | `scan()` vs `Artisan::call` output | identical channel lists | implemented | 2026-09-26 |
| RP-DEBUG-08 | The screen in the browser: save for this browser, the override live on another page's rsxapp and PHP output, Reset | playwright | `/_sys/debug-flags` as user 1 | - | planned (verified by rsx:debug --eval on 2026-09-26: rsxapp.console_debug of a fetched `/` carried the override; PHP `[DISPATCH]` lines appeared only under a specific-DISPATCH override and vanished after Reset) | 2026-09-26 |
| RP-GRID-01 | `_Sys_DataGrid_Abstract::fetch()` over a `DB::table()` source: response shape, an allowed sort, paging, rows as arrays | php | `Sys_DataGrid_Table_Fixture` over seeded login_users | keys, totals, email order across pages | implemented | 2026-09-26 |
| RP-GRID-02 | The sort allow-list is fail-closed and a bad order falls back | php | unlisted real column, injection-shaped key, empty key; order 'sideways' | sort `id`, order `desc` | implemented | 2026-09-26 |
| RP-GRID-03 | per_page clamped into [1, max]; page past the end = last page; page below 1 = first; an empty set | php | per_page 0/1000, page 99/-5 | 1, 3, last page, first page, zeros | implemented | 2026-09-26 |
| RP-GRID-04 | The tie-break orders rows sharing the primary sort value | php | sort by `status_id` (all equal) | ids desc | implemented | 2026-09-26 |
| RP-GRID-05 | An Eloquent source serializes through the model | php | `Sys_DataGrid_Model_Fixture` | no `password`, `__MODEL`, enum label | implemented | 2026-09-26 |
| RP-GRID-06 | A row-list source: null first, natural text, numeric compare, tie-break, paging, clamp, filter, `__transform_records()` | php | `Sys_DataGrid_Rows_Fixture` | the expected id orders | implemented | 2026-09-26 |
| RP-GRID-07 | A `__build_query()` answer that is no source fails loud | php | a string source | throws naming `__build_query()` | implemented | 2026-09-26 |
| RP-GRID-08 | The grid shell in the browser: search debounce, filters, sort, paging, hash state and restore, loading/empty/rows states | playwright | the Tasks history grid | - | planned (the Tasks history grid is the first shipped grid; its hash-filtered states verified by rsx:debug screenshots on 2026-09-26) | 2026-09-26 |
| RP-LOGS-01 | The Logs listing is the log directory's top-level regular files - no dotfile, subdirectory or symlink (in or out) - with size, `bytes_to_human()`, ISO modified, format and compression | php | fixture dir via `_Sys_Log_Reader::$directory_for_tests` | two names; the facts | implemented | 2026-09-26 |
| RP-LOGS-02 | A file name resolves only through the listing: traversal, absolute path, symlink out, symlink in, subdirectory, nested file, dotfile, unknown, empty and NUL-suffixed names are all refused, and `read` answers them not_found | php | `_Sys_Log_Reader::resolve()` + `_Sys_Logs_Controller::read` | null / Error_Response for each; the realpath for a listed name | implemented | 2026-09-26 |
| RP-LOGS-03 | Tail then backward pages by byte offset: the tail ends at the size, each page ends at the previous start, pages cross the 64KB chunk boundary and cover the file exactly once to byte 0; a mid-file window; `before` clamped; an unterminated last line shown; lines clamped into [1, 1000] | php | a 1000-line, 100-byte-per-line fixture | the expected offsets and lines | implemented | 2026-09-26 |
| RP-LOGS-04 | Follow reads the complete lines appended after the offset, leaves an unterminated line for the next poll, reports `more` on a full page, and the endpoint forwards to it | php | appends to a fixture file | the appended lines, offsets, end | implemented | 2026-09-26 |
| RP-LOGS-05 | Follow detects rotation: truncate-and-rewrite (same inode, new head), shrink below the offset, rename-and-recreate (new inode); an untouched file is not rotated | php | the three rotations | `rotated` true, then false | implemented | 2026-09-26 |
| RP-LOGS-06 | A .gz reads as its decompressed stream with the plain file's offsets and pages; only the tail knows the size; no identity; `follow` refuses it | php | `gzencode` of the fixture | equal windows; size null on a page; Error_Response | implemented | 2026-09-26 |
| RP-LOGS-07 | laravel entries group a `[time] env.LEVEL:` header with the lines under it; a window opening inside an entry widens to its header within a page, else opens on a `continued` entry; the filter keeps an entry whose trace or header matches without moving offsets; csp lines pretty-print (blank dropped, non-JSON kept); invalid UTF-8 scrubbed | php | a synthetic laravel.log, csp_violations.log, worker.log | the entry shapes | implemented | 2026-09-26 |
| RP-LOGS-08 | The read endpoint carries the listing's facts plus the window; the file grid searches and sorts names | php | `read` with lines 5000; `datagrid_fetch` filter/sort | 1000 lines; `app.log` | implemented | 2026-09-26 |
| RP-LOGS-09 | The viewer in the browser: tail, Load earlier, filter, Follow appending, rotation notice, not-found | playwright | `/_sys/logs/<file>` | - | planned (verified by rsx:debug --eval runs and screenshots on 2026-09-26: Load earlier 200 -> 400 lines, a Follow-appended entry with its trace grouped, the rotation notice after a truncate) | 2026-09-26 |
| RP-DASH-01 | The dashboard summary carries exactly the tile keys with the right types | php | `_Sys_Dashboard_Controller::summary` as user 1 | the nine keys; counts are non-negative ints | implemented | 2026-09-26 |
| RP-DASH-02 | Pending mail is install-wide: a PENDING row under a second site is counted for a developer on site 1, a SENT row is not | php | raw `_email_queue` rows on a new site | +1 | implemented | 2026-09-26 |
| RP-DASH-03 | The site count excludes site 0 and counts a new site | php | `sites` live rows vs the summary | live - 1; then +1 | implemented | 2026-09-26 |
| RP-DASH-04 | Failed tasks = one-shot FAILED rows with `completed_at` in the last 24h | php | FAILED 2h ago, FAILED 2d ago, COMPLETED 2h ago | +1 | implemented | 2026-09-26 |
| RP-TASKS-01 | `running` lists every RUNNING row and nothing else, datetimes ISO UTC, class basename, cron flag | php | planted running + pending rows | running listed, pending not; `...Z` times | implemented | 2026-09-26 |
| RP-TASKS-02 | History filters: status, class, since (finished within, by completed_at), search on class or method (LIKE wildcards literal); unknown filter values filter nothing; list omits params/logs; `class_options` labels by basename | php | planted failed/completed rows, two classes | the expected id sets | implemented | 2026-09-26 |
| RP-TASKS-03 | `detail` decodes params/result, splits logs, answers can_kill/can_redispatch; a missing id is not_found | php | a planted failed row; max id + 1000 | decoded row; not_found | implemented | 2026-09-26 |
| RP-TASKS-04 | `kill` requires an explanation (field error, row untouched), refuses a non-running or missing row, and settles a running row through Task_Killer | php | a planted RUNNING row with no pid | validation; validation; not_found; `killed_no_process`, KILLED + reason | implemented | 2026-09-26 |
| RP-TASKS-05 | `redispatch` refuses completed, running and schedule-tracker rows; a failed one-shot is dispatched as a NEW pending row with the same class/method/params/queue, the old row untouched | php | planted rows of `Sys_Tasks_Probe_Service` | validation x3; new row | implemented | 2026-09-26 |
| RP-TASKS-06 | `schedules` lists one row per `#[Schedule]` declaration: phrase + cron form, concurrency mode, tracker null when none, tracker datetimes ISO UTC, error excerpt first line capped at 120, `failing` exactly at `rsx.tasks.failing_schedule_warn_after` | php | framework declarations with their trackers deleted / planted in the transaction | row count; null tracker; ISO; `xxx...`; failing false at threshold-1, true at threshold | implemented | 2026-09-26 |
| RP-TASKS-07 | `run_schedule_now` dispatches a declared schedule as a new one-shot (declared queue, no params) and leaves the tracker's `next_run_at`; a second run of an `#[Exclusive]` task coalesces onto the first; an undeclared class/method is not_found | php | an unmanaged and an exclusive framework declaration | `dispatched`, new PENDING row, tracker unchanged; `coalesced` with the same id; not_found | implemented | 2026-09-26 |
| RP-EMAIL-01 | `summary` counts every status across ALL sites (words, enum order), finds the oldest pending row on another site, reports mode and transport | php | PENDING (backdated) + FAILED rows on a new site | pending +1, failed +1, total +2, oldest = the planted row | implemented | 2026-09-26 |
| RP-EMAIL-02 | Queue grid across sites: site / status word / category word filters, unknown words filter nothing, search on to_address, dev_original_to and subject (LIKE wildcards literal); rows carry status/category words, site name, error first line, no bodies; `site_options` / `status_options` | php | two rows on a new site | the expected id sets and keys | implemented | 2026-09-26 |
| RP-EMAIL-03 | `detail` returns another site's row: preview document with the CSP meta first in `<head>` (a head is created when absent), text body, resend flags; an unbuilt row has no preview; a missing id is not_found | php | BLOCKED rendered row, PENDING unrendered row, max id + 1000 | as described | implemented | 2026-09-26 |
| RP-EMAIL-04 | Catcher lookup in aiosmtpd mode: files whose HEADER carries this row's `X-RSX-Email-Id`, newest first; a body mention and another id never match; absent Maildir = `exists` false; no catcher block in other modes | php | a scratch Maildir via `rsx.mail.catcher_maildir` | `['new/newer','cur/older']`; null in suppressed | implemented | 2026-09-26 |
| RP-EMAIL-05 | `resend` (via `Rsx_Mail::resend()`): PENDING/SENDING refused untouched, BLOCKED refused without force and queued with it, FAILED reset (attempts 0, error cleared, due now, site kept); missing = not_found | php | rows on a new site | validation x3, then PENDING | implemented | 2026-09-26 |
| RP-SMS-01 | SMS `summary` counts every status across ALL sites (words, enum order), finds the oldest pending row on another site, reports `Rsx_Sms::delivery_mode()` | php | PENDING (backdated) + FAILED rows on a new site | pending +1, failed +1, total +2, oldest = the planted row | implemented | 2026-09-26 |
| RP-SMS-02 | SMS grid across sites: site / status word / category word filters, unknown words filter nothing, search on to_number, dev_original_to and body (LIKE wildcards literal); rows carry status/category words, site name, error first line, no full error or transport response; `site_options` / `category_options` | php | two rows on a new site | the expected id sets and keys | implemented | 2026-09-26 |
| RP-SMS-03 | SMS `detail` returns another site's row whole (words, site name, body, error); a missing id is not_found | php | BLOCKED row on a new site, max id + 1000 | as described | implemented | 2026-09-26 |
| RP-SITES-01 | The Sites grid lists tenants only - never site 0, never a soft-deleted site - with `member_count` = live memberships (enabled or not; a soft-deleted one excluded), created_at ISO UTC; the `enabled` filter, an unknown value filtering nothing, search on slug and on name | php | sites and `users` rows planted in the transaction | the expected id sets; counts 2 and 0; total = live tenants | implemented | 2026-09-26 |
| RP-SITES-02 | `detail` answers a tenant's counts, `field_lengths` and `can_disable`; not_found for site 0, a soft-deleted site and a missing id; `members_fetch` lists live memberships by role then id with the identity's email, the role label, `is_active` (User_Model's definition) and ISO last_login, searches email, filters the membership's own switch, and refuses site 0 | php | a site with enabled, disabled, deleted and invitation memberships | as described | implemented | 2026-09-26 |
| RP-SITES-03 | `rename` validates on the server: blank and over-length name and slug are field errors, another site's slug and a soft-deleted site's slug are taken, a refusal writes nothing, a valid rename (name at exactly the column length) is saved, keeping its own slug is not a duplicate, site 0 is not_found | php | three sites, one soft-deleted | validation x4, then saved | implemented | 2026-09-26 |
| RP-SITES-04 | `set_enabled`: disabling makes the site's membership inactive (`User_Model::is_active()`), enabling restores it; site 0 is not_found and stays enabled; the acting session's own site is refused and offers no Disable; a non-boolean is refused | php | a site with one member | false then true; not_found; validation x2 | implemented | 2026-09-26 |
| RP-USERS-01 | The Users grid lists live login identities (never a soft-deleted one) with the status word + label, a boolean developer flag, no password, and `membership_count` = live memberships on any site (enabled or not; a soft-deleted one excluded); the `status` and `developer` filters, unknown values filtering nothing, search on email; `status_options` = the enum words in order | php | identities and memberships on three sites planted in the transaction | the expected id orders; counts 2 and 0; total = live identities | implemented | 2026-09-26 |
| RP-USERS-02 | `detail` answers the identity's facts (`is_self` only for the acting identity, no password) and every live membership across every site - none on the session's site - with site name, role label, `User_Model::is_active()` (a disabled site makes an enabled membership inactive), `site_is_deleted`/`has_site_screen`, 2FA-required, API access and the invite state (an unaccepted invitation past expiry = expired; no invite code); not_found for a soft-deleted, missing, zero and non-numeric id, on `detail`, `sessions`, `signins_fetch` and `security` | php | an identity with memberships on an enabled, a disabled and a deleted site plus an invitation and a deleted membership | as described | implemented | 2026-09-26 |
| RP-USERS-03 | `sessions` answers the ACTIVE sessions of the identity (an inactive one gone), most recently active first, with site id + name, type label, IP, parsed device, ISO datetimes, `is_current`, the impersonator's id and email; no session, CSRF or handoff token | php | three `_sessions` rows, one impersonated, one inactive | two rows in order; the columns | implemented | 2026-09-26 |
| RP-USERS-04 | The sign-in grid pages the WHOLE history (30 rows: 25 + 5, where `get_history_for_user()` answers the newest 10), newest first, each row in `Login_History::present_record()`'s shape with ISO created_at; search on IP | php | 30 `_login_history` rows | totals, page sizes, keys | implemented | 2026-09-26 |
| RP-USERS-05 | `security` answers 2FA enabled, passkey held, the recovery-code COUNT, the confirmed factors only (no recovery code, no unconfirmed enrollment) and the connected provider - and no secret: no `secret`/`credential_key`/`counter`/`provider_user_key`/`avatar_url` key, and no planted seed, key, hash or provider key VALUE anywhere in the JSON; an identity with nothing answers empty | php | TOTP, passkey, two recovery codes, an unconfirmed TOTP and an SSO link planted | as described | implemented | 2026-09-26 |
| RP-USERS-06 | `set_status` writes the status a word names (updated_by = the acting developer); an unknown word is ERROR_VALIDATION; the acting identity is REFUSED (ERROR_VALIDATION naming why, status unchanged); not_found for a missing identity; `detail` publishes `status_refusal` on the acting identity only, and `status_options` | php | an identity; identity 1 as the acting developer | as described | implemented | 2026-09-26 |
| RP-USERS-07 | `set_membership_enabled` switches a membership on another site off (`is_active` false, site untouched) and on; malformed `enabled` is ERROR_VALIDATION; a soft-deleted or non-numeric membership is not_found; disabling the membership the acting session is on is REFUSED (enabling it is not); `detail` marks it `is_current` with its `disable_refusal` | php | a membership on a new site, a deleted one, users row 1 | as described | implemented | 2026-09-26 |
| RP-USERS-08 | `terminate_session` ends one session through `Session::developer_terminate_sessions()` (session.terminated fires once: actor 1, scope admin); an ended, another identity's or malformed session id is not_found; the current session is REFUSED and stays active; `terminate_all_sessions` ends the rest (0 when none) and on the acting identity spares the current session; `sessions` publishes `terminate_refusal` on the current session only | php | three rows of an identity, one of another, one of identity 1 | as described | implemented | 2026-09-26 |
| RP-USERS-09 | `remove_two_factor`: a foreign or unknown credential id is not_found (the foreign row untouched); removing one factor keeps the recovery codes while a factor remains and drops them with the last; no `credential_id` removes every credential; the acting identity is REFUSED and keeps its factor; `security` publishes `refusal` on the acting identity only | php | TOTP, passkey, two recovery codes, a stranger's TOTP | as described | implemented | 2026-09-26 |
| RP-USERS-10 | `unlink_sso`: a foreign link id is not_found; one link, then every link, answers the providers left (1, 0); the acting identity is REFUSED and keeps its link; not_found for a missing identity | php | two links, a stranger's link, one link of identity 1 | as described | implemented | 2026-09-26 |
| RP-IMP-01 | `sign_in_as` REFUSES (ERROR_VALIDATION naming why, nothing begun) the acting identity, a developer, an identity with no ACTIVE membership (disabled membership; enabled membership on a disabled site) and a session already impersonating (refused before `begin_impersonation()` would throw); `detail` publishes the same `impersonate_refusal`; a suspended identity is not refused (status is app vocabulary); not_found for a missing identity | php | identities planted per case; `cli_set_impersonator_login_user_id()` for the nesting case | as described | implemented | 2026-09-26 |
| RP-IMP-02 | `sign_in_as` requires `site_id` to be one of the identity's ACTIVE memberships' sites: blank, foreign, a disabled membership's site and a malformed value are field errors on `site_id`, nothing begun | php | an identity on site 1 plus a disabled membership elsewhere | as described | implemented | 2026-09-26 |
| RP-IMP-03 | `sign_in_as` on another site's membership answers `{destination: '/'}`, stores the developer's site under `sys.impersonation.return_site`, moves the session to the chosen site and begins: impersonator = developer, effective identity = target, start stamped | php | an identity with memberships on site 1 and a new site | as described | implemented | 2026-09-26 |
| RP-IMP-04 | `stop`: not impersonating -> redirect `/`, nothing changed; after `sign_in_as` -> impersonation ended, the developer's identity and stored site restored, the value forgotten, redirect to `_Sys_User_View_Action` of the target | php | an identity on a new site | as described | implemented | 2026-09-26 |
| RP-IMP-05 | `stop` refuses (unauthorized) an impersonation whose IMPERSONATOR is not a developer and leaves it running (impersonator and effective identity unchanged) | php | a non-developer principal impersonating another identity | as described | implemented | 2026-09-26 |
| RP-IMP-06 | At the dispatch seams, a signed-in non-developer is refused `sign_in_as` through the browser transport (is_sysadmin) and reaches the stop route through the page dispatcher (is_logged_in), which redirects to `/` | php | a non-developer identity on site 1 | unauthorized envelope; 302 to `/` | implemented | 2026-09-26 |
| RP-IMP-07 | The stop route is in `rsx.always_published_routes`, resolves in the manifest to `/_sys/stop-impersonating`, and its published grant is 1 signed in and an explicit 0 anonymous | php | - | as described | implemented | 2026-09-26 |
| RP-IMP-08 | The browser path: the dialog (site select only with 2+ active memberships), the `_sessions` row's impersonator columns and the stored return site, the application's `Impersonation_Banner` naming both identities, the stop link landing on the user detail restored; and the single-tenant `Main::init()` pin (chosen site overridden; no membership on the declared site signs the session out) | playwright | - | as described | deferred - verified by rsx:debug during the build; a playwright test cannot see fixture identities (tests/CLAUDE.md, browser tests) and would have to mint and remove its own on the dev database | 2026-09-26 |
