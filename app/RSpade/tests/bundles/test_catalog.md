# Bundles - test catalog

| ID | Purpose (what it proves) | Type | Input | Expected (approx) | Status | Last updated |
|----|--------------------------|------|-------|-------------------|--------|--------------|
| BUNDLE-WATCH-01 | A `watch` entry resolving to a FILE is registered and invalidates the bucket of the DECLARING bundle, even though the file lives outside any `/vendor/` path; and the invalidation round-trips | asset | Synthetic vendor-bucket asset bundle whose SCSS imports an outside-vendor variables file declared in `watch`; edit the variables file, then revert | Vendor CSS artifact recompiles, content-hashed filename changes, new value present; revert restores both the filename and the content | implemented | 2026-08-24 |
| BUNDLE-WATCH-02 | A `watch` entry resolving to a DIRECTORY still invalidates (no regression from the file branch) | asset | Edit a file inside the watched directory | Vendor CSS artifact filename changes | implemented | 2026-08-24 |
| BUNDLE-WATCH-03 | Dual membership: one path that is a `watch` target of a vendor-bucket bundle AND a direct `include` of an app-bucket bundle invalidates BOTH buckets from one edit | asset | Edit the shared SCSS file | Both vendor and app CSS filenames change | implemented | 2026-08-24 |
| BUNDLE-WATCH-04 | A `watch` target that does not exist fails the build loudly, naming the declaring bundle class and the path | asset | Bundle declaring a `watch` path that is neither a file nor a directory | `RuntimeException` naming the class and the path | implemented | 2026-08-24 |
| BUNDLE-STUB-01 | A JS model class carries its generated `Base_` stub into a bundle that contains neither the PHP model file nor a models directory - the reach is structural, not a side effect of an unrelated include | php | Synthetic file set holding only `File_Attachment_Model.js`, plus a manifest index registering its stub | The stub path is resolved | implemented | 2026-09-06 |
| BUNDLE-STUB-02 | The same stub is resolved once however many JS classes extend it (a duplicate would redeclare `class Base_X_Model` in the bundle) | php | Two JS classes extending one stub, plus the stub file itself, in one set | Exactly one resolved path | implemented | 2026-09-06 |
| BUNDLE-STUB-03 | Only a class extending a REGISTERED stub pulls one in - the mechanism keys on the stub registry, never on a `Base_` name shape | php | A JS class extending `Base_Thing_That_Is_Not_A_Model` | Nothing resolved | implemented | 2026-09-06 |
| BUNDLE-STUB-04 | A registered stub missing from disk fails loud naming the JS class file that needs it, instead of compiling a bundle with its model layer dead | php | Manifest index naming a stub path that does not exist | `RuntimeException` containing `missing from disk` | implemented | 2026-09-06 |
| BND-CDN-01 | A compiled bundle stylesheet reaches no external host: an absolute `url()` in application SCSS is mirrored and rewritten to `/_vendor/<name>` | asset | Scratch module bundle whose app SCSS names a `file://` png | The compiled app CSS contains `/_vendor/{md5}_pixel-temp.png` and no `file://`; the mirror file exists byte-identical | implemented | 2026-09-01 |
| BND-CDN-02 | A remote `@import` in application SCSS is fetched, localized against ITS OWN url, and spliced into the bundle | asset | Same fixture, `@import url("file://.../imported-temp.css")` whose body names a `file://` woff2 | No `@import` survives; `.rsx_cdn_fixture_imported` is inline; the nested url is `/_vendor/{md5}_glyph-temp.woff2`; `format("woff2")` untouched | implemented | 2026-09-01 |
| CONCAT-01 | JavaScript files are joined in the caller's order, each behind its own `/* === path === */` banner, with a decodable version-3 inline sourcemap naming every input | asset | Two scratch `.js` files through `Concatenator::concat_js()` | Both banners present, order preserved, base64 sourcemap decodes and names both sources | implemented | 2026-09-03 |
| CONCAT-02 | A file READ from one path but ATTRIBUTED to another (a babel transform) gets the `(babel)` banner and maps to the developer's own file, never the temp file | asset | `['path' => transformed, 'source' => original]` | `(babel)` banner names the original; sourcemap sources contain the original and NOT the transformed temp file | implemented | 2026-09-03 |
| CONCAT-03 | CSS files are joined with banners and their own inline sourcemap comment | asset | Two scratch `.css` files through `Concatenator::concat_css()` | Both rules present, banner present, `/*# sourceMappingURL=...` tail present | implemented | 2026-09-03 |
| CONCAT-04 | **THE REGRESSION TEST.** A file list far past the single-argument ceiling concatenates successfully - the failure that took a downstream app's every route to a 500 cannot recur | asset | 1,500 padded scratch files (~150,000 bytes of equivalent argv, ~19,000 past the 131,072-byte MAX_ARG_STRLEN); asserts the equivalent argv length exceeds the ceiling first | All 1,500 concatenated, one banner each, first and last file present | implemented | 2026-09-03 |
| CONCAT-05 | A sourcemap describing more generated lines than its file has is refused, naming the file (unchecked, each phantom line becomes a bare `undefined` at bundle top level and throws on execution) | asset | One-line file carrying a 5-line map | `RuntimeException` containing `Malformed sourcemap` and the filename | implemented | 2026-09-03 |
| CONCAT-06 | A missing input file fails loudly and names the file | asset | A path that was never written | `RuntimeException` naming the file | implemented | 2026-09-03 |
| CONCAT-07 | An empty file list is a compiler bug, not an empty artifact | asset | `concat_js([], ...)` | `RuntimeException`, no artifact | implemented | 2026-09-03 |
| CONCAT-08 | The node service is reachable after a concatenation - i.e. concat spawned it on demand and left it serving | asset | `Rsx_Node_Service::ping()` after a concat | true | implemented | 2026-09-04 |
| CONCAT-09 | A concatenated CSS rule maps to its ORIGINAL source file and line - pins the header off-by-one and the column-0 line-probe remap defects that sent devtools to the wrong component stylesheet | asset | decode the merged base64-VLQ map, assert three probes (mapped chunk x2, identity chunk x1) | true | implemented | 2026-09-04 |
| ARGV-GUARD-01 | The single-argument ceiling constant is the kernel's 32 pages, not a chosen number | php | `ARG_MAX_SINGLE_BYTES` | equals 131072 and `32 * 4096` | implemented | 2026-09-03 |
| ARGV-GUARD-02 | `exec_safe()` refuses an over-limit command legibly - byte count, limit, `MAX_ARG_STRLEN`, and that `ulimit` is not the answer | php | A command one byte past the ceiling | `RuntimeException` naming all four | implemented | 2026-09-03 |
| ARGV-GUARD-03 | A command just under the ceiling still runs - the guard sits exactly at the OS limit and refuses nothing that could have worked | php | Command ~4KB under the ceiling | Exit 0, expected stdout | implemented | 2026-09-03 |
| ARGV-GUARD-04 | An ordinary short command is unaffected by the guard | php | `printf` | Exit 0, expected stdout | implemented | 2026-09-03 |
| SCSS-RPC-01 | Development compile: variables/nesting resolve, output is expanded, and a decodable v3 sourcemap is embedded with no surviving `file://` protocol | asset | Scratch `.scss` with a variable and a nested rule | `#ff0000`, `.scss_fixture .nested`, expanded output, decodable map | implemented | 2026-09-04 |
| SCSS-RPC-02 | Production compile is compressed AND the postcss pass (autoprefixer + cssnano) actually ran | asset | Scratch `.scss` carrying a comment, compiled with production=true | Comment gone, colour shortened, output essentially one line | implemented | 2026-09-04 |
| SCSS-RPC-03 | `@import` resolves - the mechanism the bundler's master file depends on (one entry importing every stylesheet by absolute path) | asset | Entry importing a partial by absolute path | The partial's rule is present in the output | implemented | 2026-09-04 |
| SCSS-RPC-04 | Invalid SCSS fails loudly naming the input file, and leaves NO artifact (the stylesheet is written before it is optimized, so file presence proves nothing) | asset | `.scss` referencing an undefined variable | `RuntimeException` naming the file; no output file on disk | implemented | 2026-09-04 |
| SCSS-RPC-05 | The node service spawns on demand for an SCSS compile and answers a ping | asset | `Rsx_Node_Service::ping()` after a compile | true | implemented | 2026-09-04 |
| SCSS-RPC-06 | The generated-script idiom is gone: no `compile.js` beside the input, and the processor no longer shells out | asset | A compile, then inspect the directory and the processor source | No `compile.js`; no `shell_exec` in the processor | implemented | 2026-09-04 |
| BUILD-HASH-01 | The development build hash notices a same-size edit made inside one clock second (today it cannot - `path+size+mtime` at 1s granularity serves a stale artifact; see `issues_encountered.md`) | asset | Same-length edit plus an immediate recompile, without advancing mtime | The artifact is invalidated | planned (owner decision: make dev hashing content-based) | 2026-09-03 |
| BUNDLE-INCLUDE-01 | A module bundle cannot include another module bundle | php | Module bundle including a module bundle | Fatal with remediation text | planned | 2026-08-24 |
| BUNDLE-INCLUDE-02 | An asset bundle discovered via directory scan cannot declare directory includes | php | Scanned asset bundle with a directory in `include` | `RuntimeException` from `validate_no_directory_scanning` | planned | 2026-08-24 |
| BUNDLE-NPM-01 | Changing an `npm` declaration invalidates the vendor output | asset | Add/remove an npm entry | Vendor JS filename changes | planned | 2026-08-24 |
