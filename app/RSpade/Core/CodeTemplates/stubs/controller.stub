<?php

namespace {{ namespace }};

use Illuminate\Http\Request;
use App\RSpade\Core\Controller\Rsx_Controller_Abstract;
use Route;

/**
 * {{ controller_class }} - server-rendered pages for the {{ page_title }} feature.
 *
 * The class-level gate covers every surface in the class; #[Auth] is mandatory
 * (the manifest build fails without it). A page that must be reachable by an
 * anonymous visitor declares #[Auth('public')] instead.
 */
#[Auth('is_logged_in')]
class {{ controller_class }} extends Rsx_Controller_Abstract
{
    /**
     * {{ feature_description }}
     *
     * @param Request $request
     * @param array $params
     * @return mixed
     */
    #[Route('{{ route_path }}')]
    public static function index(Request $request, array $params = [])
    {
        $data = [
            // Add your data here
        ];

        return rsx_view('{{ view_path }}', $data);
    }
}
